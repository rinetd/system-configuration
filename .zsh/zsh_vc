function mk-svndir()
{
    mkdir -vp branches tags version trunk/{doc/,lib,patch,source}
}


function gtags-gen()
{
    if [[ $# == 0 ]]; then
        find -L ./ -name '*.[chy]' > gtags.files
    else
        find -L $*  -name '*.[chy]' > gtags.files
    fi

}



