function mk-svndir()
{
    mkdir -vp branches tags version trunk/{doc/,lib,patch,source}
}


function gtags-gen()
{
    if [[ $# == 0 ]]; then
        find ./ -name '*.[chy]' > gtags.files
    else
        find $*  -name '*.[chy]' > gtags.files
    fi

}



