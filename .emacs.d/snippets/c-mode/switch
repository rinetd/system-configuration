# -*- mode: snippet -*-
# name: switch
# key: case
# --
switch (${1:ch}) {
       case ${2:const}:
       ${3:a = b};
       break;
       default:
       ${4:action};
       break;
} /* end of switch case */
